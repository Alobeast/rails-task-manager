<ul>
<% @tasks.each do |task| %>
<li><%= task.title %> - <%= task.details %>
  <%= link_to "Go see!", task_path(task.id) %> <%= link_to "Delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %></li>
<% end %>
</ul>
<%= link_to "Create new task!", tasks_new_path %>
